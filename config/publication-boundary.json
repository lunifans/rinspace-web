{
  "schemaVersion": 1,
  "allowedEnvironmentFiles": [".env.example"],
  "productionDomainAllowlist": [
    {
      "path": "config/runtime.official.example.json",
      "reason": "Documented browser-public official deployment template."
    },
    {
      "path": "src/app/config/runtime.ts",
      "reason": "Fail-closed validation rejects official domains outside official mode."
    },
    {
      "path": "src/platform/http/index.ts",
      "reason": "Provider adapter derives the CloudBase auth origin from validated public config."
    },
    {
      "path": "src/services/phoneAuth.ts",
      "reason": "Legacy provider adapter derives the CloudBase auth origin from validated public config."
    },
    {
      "path": "src/utils/internalContentPreview.ts",
      "reason": "URL parser recognizes the canonical site to keep internal previews on an allowlisted path."
    }
  ],
  "demoSurfacePrefixes": [
    "config/runtime.demo.json",
    "config/runtime.demo.subpath.json",
    "src/demo/"
  ],
  "policy": "Secrets and production origins are forbidden from demo surfaces. Any production-domain source occurrence must have a narrow reviewed allowlist entry."
}
