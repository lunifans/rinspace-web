[build]
command = "pnpm build && pnpm prepare:netlify"
publish = "netlify-dist"

[build.environment]
NODE_VERSION = "22.22.3"
PNPM_VERSION = "9.7.0"
GENERATE_SOURCEMAP = "false"

[build.processing]
skip_processing = true

[dev]
framework = "#static"
publish = "netlify-dist"
